# FindIt: Rebuild 개발 현황 & 작업 체크리스트

> 작성일: 2026-04-24 (초기) / 2026-04-25 (1.1 서버 인프라 구현 완료)
> 기준 문서: `docs/` 폴더 내 기획서 (2012–2013) + `android_project/` 현행 코드

---

## 📌 진행 로그

### 2026-04-25 — 1순위 1.1 서버 인프라 구현 완료
- Node.js + Express + MySQL 기반 서버 스켈레톤 작성 (`server/`)
- 13개 REST 엔드포인트 구현
- TCP 소켓 서버 (코드 100~107) 구현, 프레임 프로토콜 포함
- 8개 DB 테이블 스키마 작성 + 시드 데이터
- Docker Compose (로컬 MySQL) + GCP Cloud Run 배포 가이드
- 모듈 로드/프로토콜 라운드트립/소켓 E2E 스모크 테스트 통과
- 상세 문서: [server/README.md](../server/README.md)

### 2026-04-25 — 1순위 1.2, 1.3, 1.4 구현 완료
- **1.2 클라 서버 주소 중앙화**: `Property.java` 단일 소스화, 하드코딩 39건 → `Property.SERVER_DOMAIN` 참조로 일괄 교체. `AndroidManifest.xml` + `network_security_config.xml` 로 HTTPS 강제 + 로컬 개발 예외 도메인 허용.
- **1.3 회원 시스템**: 서버 측 유효성 검증 유닛 테스트 10/10 통과, Guest/Facebook 로그인 엔드포인트, opaque 세션 토큰 발급 + 중복 로그인 기존 세션 강제 로그아웃, `/logout.json` 엔드포인트.
- **1.4 게임 핵심 로직**: `FINDRECT_TOTALNUM` 10→7, 점수 공식 기획 반영 (승자 +100 + 콤보 보너스), 콤보 증감 로직 추가 (정답 +1 / 오답 0 / 상대 발견 0), AI 시간 공식 `7 - level × 0.03` 초 (±15% 랜덤), HP 레벨 테이블(101 값) 도입 + 로그인/레벨업 연동.

### 2026-04-25 — 2순위 전체 구현 완료
- **Analytics**: `event_log` 테이블 + `POST /app/member/event.json` 이벤트 수집, `/admin/analytics/{overview,dau,revenue,events,ads}` 집계 API, `members.last_active_at` 자동 갱신
- **광고 관리**: `ad_images.click_count` + `ad_click_log` 추가, `POST /app/member/adClick.json`, `/admin/analytics/ads` 에 CTR 계산
- **어드민 대시보드 HTML**: `public/admin.html` 단일 파일 SPA, `GET /admin/ui` 에 공개 서빙, X-Admin-Token 은 localStorage 저장 (개요/통계/광고/공지/푸시 탭)
- **FCM 푸시**: `members.fcm_token` + `push_log` 테이블, `POST /app/member/registerFcmToken.json`, 어드민 `POST /admin/push/user/:id` 와 `/broadcast`, 소켓 107(초대) 코드가 **오프라인 타겟에게 자동 푸시**. firebase-admin 은 optionalDependency (미설치 시 dev log 로 fallback)
- **reCAPTCHA**: `util/recaptcha.js` 미들웨어 — `/join.json`, `/guest.json` 에 적용. `RECAPTCHA_SECRET` 환경변수 미설정 시 no-op (개발 편의)
- **IAP 재검증**: `util/iap.js` (Google Play Developer API + Samsung Checkout), `POST /app/member/verifyIap.json` — 중복 토큰 차단 + verified=true 시에만 재화 지급. 상품 카탈로그 (Samsung 구 SKU 호환). googleapis 는 optionalDependency
- **안정성**: `uncaughtException`/`unhandledRejection` 핸들러, DB `connectTimeout: 3000` — DB 없어도 서버 프로세스 유지

### 2026-04-25 — 4. 번역/국제화 (i18n) 완료
- `번역할 한국어.txt` 135줄 전 항목 매핑 → `res/values-ko/strings.xml` 신규 생성 (145 키)
- `res/values/strings.xml` 누락분 4개 추가 (`rank`, `score`, `payment_complete`, `view_more`)
- 영/한 키 셋 100% 일치 확인 (`diff` 무차이)
- 은/금 반지 효과 영문 오역 수정 (increases → reduces)
- 코드 내 한글 하드코딩 제거: `InventoryActivity_Google/SAMSUNG.java` Toast/리스트, `rank_user.xml`, `rank_listrow.xml`
- 디바이스 언어가 한국어면 `values-ko`, 그 외엔 `values` 자동 폴백 (Android 표준 리소스 시스템)

### 2026-04-25 — 1순위 1.5~1.13 구현 완료
- **DB 스키마 확장**: `skill_catalog`, `item_upgrade_log`, `admin_tokens`, `ad_reward_log` 테이블 추가. `wallets` 에 `inventory_size`, `skill_deck_count` 컬럼 추가. 마이그레이션 스크립트 포함.
- **1.5 스킬 트리**: 44개 스킬 시드 (`seed_skills.sql`), 선행 스킬 CSV + unlock_level + point_cost, `GET /app/member/skillCatalog.json`, `POST /app/member/learnSkill.json` (레벨/포인트/선행 검증).
- **1.6 재화/아이템**: `spendGem` / `addGem` / `upgradeItem` (확률 기반, 플럭스 보정) / `expandInventory` / `expandSkillDeck` 엔드포인트. `balance.js` 모듈 유닛 테스트 16/16 통과. 클라 DBAdapter v2: `mGem`/`mHP`/`mInventorySize`/`mSkillDeckCount` 컬럼 + ALTER 마이그레이션.
- **1.7 상점 가격표**: `Items.java` 32줄 블록 2013 스펙 적용 (pen/ring/berry/potion/gold 가격 + 효과 스케일 20단위/0.1초). `strings.xml` 코인팩 텍스트 100/250/500/650 로 변경.
- **1.9 레벨 체계**: 100레벨 누적 점수 + 공격/방어/HP + 등급(A~J) 테이블 서버 상수화. `GET /app/member/levelTable.json`.
- **1.11 광고 보상**: `POST /app/member/adReward.json` — 24h 상한 20회 + 조회당 코인 5 적립, `ad_reward_log` 감사.
- **1.12 친구 랭킹**: `GET /app/member/rankListFriends.json?userId=&fbFriends=csv` (본인+친구 순위, 어제 대비 delta). `rankListTop.json` top N 랭킹.
- **1.13 컨텐츠 관리 어드민**: `/admin/images` `/admin/ads` `/admin/notices` CRUD + `/admin/stats` 요약. `X-Admin-Token` 헤더 인증.

---

## 0. 문서 vs 현행 코드 요약

| 영역 | 기획서 (2012–2013) | 현재 코드 | Gap |
|---|---|---|---|
| 스킬 총 개수 | 44~50개 (5계열 × 5~10단계) | 서버 `skill_catalog` 44개 시드 완료 / 클라 렌더링 8개 (에셋 공유 예정) | 🟡 서버 데이터 완료, 클라 에셋 그룹 매핑 남음 |
| 재화 | 코인 + **보석(gem)** 2종 | 코인/포인트/**보석** 모두 DB+서버+클라 DBAdapter 완비 | 🟡 UI 노출 남음 |
| 레벨 | 1~100 (점수 체계 + 기본능력치 테이블) | 서버 `balance.js` 100레벨 테이블 + `/levelTable.json` 완료, 클라 HP 테이블 | 🟢 데이터 완성 (레벨업 자동 포인트만 TODO) |
| 찾는 개수 | 한 이미지당 7개 | **7개** (`FINDRECT_TOTALNUM=7`) | 🟢 일치 |
| 게임 시간 | - | **40초** (`GAMETIME=40`) | ℹ️ 코드 확정값 |
| AI 시간 공식 | `7 - (레벨 × 0.03)` | **공식 적용됨** (60fps 환산, ±15% 변동) | 🟢 |
| 콤보 보너스 | 콤보 1/2/3/4 → +100/200/400/700점, 시간 +1/2/4/7초 | `comboScoreBonus()` / `comboTimeBonus()` 구현, 증감 로직 추가 | 🟢 |
| 점수 공식 | 승자 = 찾은수×50 + 100 / 패자 = 찾은수×50 | **공식 적용됨** (콤보 보너스 포함) | 🟢 |
| 아이템 타입 | pen, ring, berry, potion, etc(인벤/스킬덱 확장) | 6종 (PEN, RING, BERRY, POTION, GOLD, ETC) | 🟢 구조 일치 |
| 아이템 업그레이드 | 1~10단계, 확률 0.5→0.1 (`findit_item,skill,shop_20120827.xlsx`) | 서버 `/upgradeItem.json` + `item_upgrade_log`, 플럭스 보정 (+5/+10%) | 🟢 |
| 인벤토리 확장 | 1~9단계 (3칸 단위 확장) | 서버 `/expandInventory.json`, 3~30칸, 비용 50→450 | 🟢 |
| 스킬덱 확장 | 1~3단계 | 서버 `/expandSkillDeck.json`, 1~4 (코드는 4까지, 스펙은 3 확장) | 🟢 |
| 상점 (IAP 코인팩) | 100 / 250 / 500 / 650 코인 ($0.99/$1.99/$3.99/$4.99) | **2013 스펙 적용 완료** (Items.java + strings.xml), 스토어 재등록만 남음 | 🟢 |
| Facebook 로그인 | 로그인 / 친구 / 초대 / 공식 페이지 | ID 필드만 존재 (`mFacebookUserId`), SDK 미연동 | 🔴 미구현 |
| Guest 로그인 | 정보 저장 X, 광고/컨텐츠 노출만 | 미확인 | 🟡 재확인 필요 |
| 친구 초대/요청 | 대기방에서 친구 초대, 아이템/코인 요청 | 버튼만 존재, 서버 API 미구현 | 🔴 미구현 |
| 공지사항 | Notice 화면 | `NoticeActivity.java` (46줄, 스켈레톤) | 🟡 구현 빈약 |
| 랭킹 | 일/주 단위, 상승/하락 표시 | `RankActivity.java` (153줄) | 🟡 부분 구현 |
| 서버 | REST + TCP 소켓 `14.63.220.39:22131` | **신규 Node.js 서버 구현 완료** (`server/`), 로컬 동작 확인 | 🟢 배포 준비 (GCP 프로젝트 생성만 남음) |
| IAP | Google / Samsung / KT / SKT | 4개 모두 디컴파일, 한국 통신사는 **서비스 종료** | 🟡 Google/Samsung만 유효 |
| 캐릭터 커스터마이징 | 사진기로 얼굴 교체 | `characters` 테이블 + 스펙 존재 | 🟢 구조 존재 |

---

## 1. 1순위 — 게임 진행 필수 요소 (기획서 "작업 우선순위 v.0.5.docx" 기반)

### 1.1 서버 인프라 🟢 (2026-04-25 구현 완료, 실제 배포만 남음)
- [x] **REST API 서버 재구축** — `/app/member/*` 14개 엔드포인트 ([server/src/routes/member.js](../server/src/routes/member.js))
  - [x] `/app/member/join.json` (회원가입) — bcrypt 해시, 지갑 초기화
  - [x] `/app/member/login.json` (로그인) — user+item+skill 일괄 반환
  - [x] `/app/member/addCoin.json` / `/app/member/spendCoin.json` — FOR UPDATE 락
  - [x] `/app/member/buyItemAndSpendCoin.json` — 트랜잭션 처리
  - [x] `/app/member/spendMyItem.json`
  - [x] `/app/member/buySkillAndSpendPoint.json` — 중복 학습 차단
  - [x] `/app/member/hitViewCount.json` (광고 조회)
  - [x] `/app/member/newImageList.json` (스테이지 이미지)
  - [x] `/app/member/newAdImageList.json` (광고 이미지)
  - [x] `/app/member/mutiAddUp.json` (멀티 매칭/집계)
  - [x] `/app/member/rankList?userId=` (랭킹) — RANK() window function
  - [x] `/app/member/notice.json` (공지) — HTML 렌더 (WebView 호환)
  - [x] `/app/member/itemPurchaseHistList?userId=` (구매 내역)
- [x] **소켓 서버** (`:22131` 대체) — [server/src/socket/server.js](../server/src/socket/server.js)
  - [x] 메시지 코드 100~107 (유저목록/방생성/나가기/입장/준비/게임/초대) 재구현
  - [x] 4자리 zero-padded 길이 prefix 프로토콜 ([protocol.js](../server/src/util/protocol.js))
  - [x] 재접속/끊김 처리 (close 이벤트에서 상대에게 102 전송)
  - [x] E2E 테스트: 2 client 접속 → 방 생성 → 입장 → 상대 캐릭터 교환 통과
- [x] **DB 스키마** ([server/sql/schema.sql](../server/sql/schema.sql))
  - [x] `members` (이메일, bcrypt 암호, 닉네임, 캐릭터, auth_type)
  - [x] `wallets` (코인, 포인트, **보석(gem)**, HP, attack, defense)
  - [x] `inventories` (보유 아이템, **upgrade_level**, quantity)
  - [x] `skills` (학습 스킬, skill_id unique)
  - [x] `rankings` (일/주 집계, delta 컬럼)
  - [x] `notices`, `images` (image_cut JSON 좌표), `ad_images`
  - [x] `iap_receipts` (Google/Samsung/Olleh/TStore 스토어 enum)
  - [x] `login_logs` (session_token + logged_out_at 활성 세션 조회)
- [ ] **GCP 배포** — 코드 + Dockerfile 예시 + 배포 커맨드는 준비됨 ([server/README.md](../server/README.md)). **최종 단계에서 진행 예정**

### 1.2 클라이언트 서버 주소 교체 🟢 (2026-04-25 완료)
- [x] `GameActivity.java` 의 `SERVER_DOMAIN` → [Property.SERVER_DOMAIN](../android_project/app/src/main/java/com/findit/battle/Property.java) 중앙 참조
- [x] `JoinActivity.java`, `RankActivity.java`, `NoticeActivity.java`, `InventoryActivity_*.java` 동일 교체 (39건 일괄 sed, 남은 5건 개별 Edit)
- [x] TCP 소켓 IP/포트: `GameActivity.SERVER_IP` → `Property.SERVER_IP`, `SERVER_PORT` → `Property.SERVER_SOCKET_PORT`
- [x] `AndroidManifest.xml` 에 `android:usesCleartextTraffic="false"` + `android:networkSecurityConfig="@xml/network_security_config"` 추가
- [x] [network_security_config.xml](../android_project/app/src/main/res/xml/network_security_config.xml) — 기본 HTTPS 강제, 로컬 개발 도메인(10.0.2.2, localhost, 192.168.*) 만 평문 허용
- [ ] 운영 GCP URL 실제 값 반영 (지금은 `http://10.0.2.2:8080/` 에뮬레이터 루프백 값)

### 1.3 회원 시스템 🟢 (2026-04-25 완료)
- [x] 가입 유효성 검증 — 이메일 정규식, 비밀번호 4~12자, 닉네임 1~20자, 캐릭터 0~2 ([validation.js](../server/src/util/validation.js), 유닛 테스트 10/10 통과)
- [x] 가입 중복 체크 — user_id 및 user_nick UNIQUE 선제 검사
- [x] 로그인 세션 토큰 — 24바이트 opaque 토큰, `login_logs` 테이블에 기록 ([session.js](../server/src/util/session.js))
- [x] Guest 입장 — `/app/member/guest.json`, `guest_<deviceId>` 또는 랜덤 ID 로 익명 계정 생성
- [x] Facebook 로그인 엔드포인트 — `/app/member/facebook.json`, `fb_<fbUserId>` 매핑 (Facebook SDK 클라 통합 및 서버측 Graph API 검증은 TODO)
- [x] 중복 로그인 검출 — `createSession()` 에서 기존 active 세션 모두 `logged_out_at = NOW()` 후 새 세션 발급
- [x] 로그아웃 엔드포인트 — `/app/member/logout.json`, 토큰 invalidate
- [ ] 클라이언트 Facebook SDK 통합 (Android 측 Facebook Login SDK 추가 — 1.3의 후속)
- [ ] 자동 로그인 (`mAutoLogin`) 서버 검증 — 토큰 재검증 엔드포인트 추가 필요

### 1.4 게임 핵심 로직 🟢 (2026-04-25 완료)
- [ ] 스테이지 이미지 다운로드 3G/4G 경고 다이얼로그 — 원본 클라 `번역할 한국어.txt:48` 문자열은 있으나 로직 연결 필요
- [x] **`FINDRECT_TOTALNUM` 10 → 7** ([Objects.java:58](../android_project/app/src/main/java/com/findit/battle/Objects.java#L58))
- [x] 점수 공식 수정 — `calculateScore()` 승자 `findNum*50 + 100`, 패자 `findNum*50` + 콤보 보너스 ([GameView.java:3025](../android_project/app/src/main/java/com/findit/battle/GameView.java))
- [x] 콤보 보너스 함수 추가 — `comboScoreBonus(combo)`: 1→100, 2→200, 3→400, 4+→700 / `comboTimeBonus(combo)`: 1→1, 2→2, 3→4, 4+→7초
- [x] 콤보 증감 로직 — 정답 터치 `mCombo++`, 오답 터치 `mCombo=0`, 상대가 찾으면 `mCombo=0`
- [x] AI 시간 공식 `7 - (본인레벨 × 0.03)` 적용 ([GameView.java:2321 getAIFindTime](../android_project/app/src/main/java/com/findit/battle/GameView.java)) — 60fps 환산 + ±15% 랜덤
- [x] HP 레벨 테이블 — `HP_BY_LEVEL[101]` 상수 ([GameView.java Result 클래스](../android_project/app/src/main/java/com/findit/battle/GameView.java))
- [x] `GameView.mHP`, `mGem` 필드 추가 + 로그인 응답에서 파싱 (`gameview.mHP = subjsonobject.optInt("hp", hpForLevel(level))`)
- [x] 레벨업 시 HP 자동 재계산 — `upAbillity()` 에서 `mHP = hpForLevel(mLevel)` 호출
- [ ] HP 게임플레이 적용 — 어떤 스킬이 HP 를 깎는지, HP 0 시 어떻게 되는지 등 룰 설계 필요 (기획서에 미정의)

### 1.5 스킬 시스템 🟢 (2026-04-25 서버 데이터 완료; 클라 렌더링은 에셋 공유 방식)
**현재**: 8개 (Puppy's footprint, Cat's claw, Explosion × 2, Flame × 2, Snowflake × 2) — 기존 `Objects.java` 의 하드코딩 애니메이션

**서버**: 44개 스킬 마스터 데이터가 `skill_catalog` 테이블에 시드됨 ([seed_skills.sql](../server/sql/seed_skills.sql))

- [x] 고양이발톱 1~9 (Lv 1~9, 공격 1.0~1.8초) — skill_id 14, 20~27
- [x] 강아지발자국 1~5 (Lv 10~14, 공격 1.9~2.3초, 선행: 고양이발톱5) — 13, 30~33
- [x] 폭발 1~5 (Lv 15~19, 공격 2.0~2.4초, 선행: 고양이발톱8+강아지발자국5) — 15, 40~43
- [x] 불꽃 1~5 (Lv 20~24, 공격 2.5~2.9초, 선행: 폭발5) — 16, 50~53
- [x] 눈꽃 1~5 (Lv 25~29, 공격 3.0~4.0초, 선행: 불꽃5) — 17, 60~63
- [x] 강력폭발 1~5 (Lv 30~34, 공격 4.1~5.0초) — 18, 70~73
- [x] 강력불꽃 1~5 (Lv 35~39, 공격 5.1~6.0초) — 19, 80~83
- [x] 강력눈꽃 1~5 (Lv 40~44, 공격 6.1~7.0초) — 90~94
- [x] 선행 스킬 의존성 그래프 — `required_skill_ids` CSV + 서버 검증 ([skills.js learnSkill](../server/src/routes/skills.js))
- [x] `/app/member/learnSkill.json` — 레벨/포인트/선행 모두 검증
- [x] 기존 `/app/member/buySkillAndSpendPoint.json` 호환 유지 (BC)
- [ ] 클라 `Objects.java` 에서 서버 카탈로그 기반 스킬 생성 (현재는 하드코딩 8개 유지, `asset_group` 으로 에셋 공유 예정)
- [ ] 클라 애니메이션 에셋 추가 (고양이/강아지/폭발/불꽃/눈꽃/강력× 3종 — 총 8 에셋 그룹이면 충분)

### 1.6 재화/아이템 시스템 🟢 (2026-04-25 구현 완료; 클라 UI 연결은 후속)
- [x] **보석(Gem) 화폐 추가**
  - [x] DB `wallets.gem` (schema.sql) + `DBAdapter.FIELD_GEM = "mGem"` 컬럼 + v1→v2 ALTER
  - [x] 서버 API: `spendGem.json` / `addGem.json` (어드민 토큰 필요)
  - [x] 로그인 응답에 `user.gem` 포함
  - [ ] 클라 상점 UI 에서 보석 결제 선택지 노출
- [x] 아이템 업그레이드 시스템 ([economy.js upgradeItem](../server/src/routes/economy.js))
  - [x] `inventories.upgrade_level` 0~9 (10단계)
  - [x] 확률 테이블 0.5 → 0.1 (단계↑ 감소) — [balance.js UPGRADE_RATES](../server/src/util/balance.js)
  - [x] 비용 100 → 900 코인 (단계↑ 비싸짐)
  - [x] `fluxUsed` 파라미터 — none/flux(+5%)/advanced_flux(+10%)
  - [x] `item_upgrade_log` 감사 테이블
- [x] 인벤토리 확장 `expandInventory.json` — 기본 3칸, 9단계×+3 = 최대 30칸, 비용 50→450 코인
- [x] 스킬덱 확장 `expandSkillDeck.json` — 기본 1개, 최대 4개, 비용 100/300/900 코인
- [ ] 플럭스/고급플럭스 아이템 자체를 상점에 노출 (현재는 파라미터로만 수용)

### 1.7 상점 가격표 업데이트 (2013 최신 스펙 적용) 🟡
**현재 코드**(`Items.java`) vs **2013 기획**(`findit_shop_items_20121220.xlsx`) 불일치:

| 항목 | 현재 코드 | 2013 스펙 | 조치 |
|---|---|---|---|
| 일반펜 | 100 코인 / +0.2초 | 50 코인 / +0.5초 | 수정 |
| 날개달린펜 | 300 / +0.4 | 100 / +0.8 | 수정 |
| 무지개펜 | 1000 / +0.7 | 200 / +1.0 | 수정 |
| 은반지 | 200 / -0.1 | 80 / -0.6 | 수정 |
| 금반지 | 500 / -0.2 | 150 / -0.9 | 수정 |
| 방어포션 | 5 / -0.2 | 10 / -0.3 | 수정 |
| 좋은방어포션 | 10 / -0.4 | 80 / -0.6 | 수정 |
| 고급방어포션 | 50 / -0.8 | 150 / -0.9 | 수정 |
| 폭발열매 | 1 / ? | 300 / +1.2 | 수정 |
| 불꽃열매 | 1 / ? | 100 / +0.5 | 수정 |
| 눈꽃열매 | 1 / ? | 200 / +0.8 | 수정 |
| 강력폭발열매 | 1 / ? | 800 / +2.2 | 수정 |
| 강력불꽃열매 | 1 / ? | 400 / +1.5 | 수정 |
| 강력눈꽃열매 | 1 / ? | 600 / +1.8 | 수정 |
| 코인팩 | 1000/2500/5500/8500 | **100/250/500/650** ($0.99/$1.99/$3.99/$4.99) | 수정 |

- [x] **[Items.java](../android_project/app/src/main/java/com/findit/battle/Items.java) 가격/효과 2013 스펙 적용 완료** (pen/ring/berry/potion/gold — power·price 배열 모두 갱신)
- [x] `strings.xml` 코인팩 텍스트 100/250/500/650 로 변경
- [ ] `strings.xml` 아이템 설명(pen01~potion22 효과 문구) 2013 스펙에 맞춰 영문 업데이트 — 텍스트 일부는 이미 존재, 수치만 재확인 필요
- [ ] **IAP 상품 ID 재등록** (Google Play / Samsung) — `Property.SAMSUNG_IAP_ITEM_ID_COIN1000~8500` 상수명은 유지되나 실제 등록 상품의 지급량은 100/250/500/650 코인으로 맞춰야 함

### 1.8 대기방 / 배틀방 / 게임 UI 🟡
**현재 존재**: `WaitingRoom.java`(415줄), `GameView.BattleRoom`(1224~1870줄), `GameView.GameRoom`(1872~3870줄)

- [ ] 대기방 기능 (`용어정리.docx` 기반)
  - [ ] 방 만들기
  - [ ] 친구 아이디 직접 초대 (아이디 입력 → 소켓 106 초대)
  - [ ] 리스트에서 친구 선택 초대
  - [ ] Facebook 친구 목록 연동 초대 (SDK 통합 필요)
  - [ ] **친구에게 아이템/코인 요청** (`친구요청` 기능 — 서버 API 신규 필요)
- [ ] 배틀방 기능
  - [ ] "기다리기" → "게임하기" 버튼 전환 (기획서 62줄 예정)
  - [ ] 스킬덱 선택 UI (`SKILLWINDOW_TOTALNUM=8` 중 5개 선택)
  - [ ] 준비 상태 동기화 (`mLeftReady`, `mRightReady`)

### 1.9 레벨/경험치/점수 체계 🟢 (2026-04-25 서버 완료)
**기획서**: `레벨별 첨수체계_기본능력치_02.xlsx` — 100레벨까지 정의
- [x] 레벨별 누적 점수 테이블 (1Lv=5000, 2Lv=10000, ... 100Lv≈6.6억) — [balance.js LEVEL_SCORE](../server/src/util/balance.js)
- [x] 레벨별 기본 공격력/방어력 (1Lv=0.5, 100Lv=1.57) — `LEVEL_ABILITY[101]`
- [x] 레벨별 체력 (1Lv=110, 100Lv=5000) — `LEVEL_ABILITY` hp 필드
- [x] 등급 시스템 A~J (10레벨 단위) — `GRADE_BANDS` + `gradeForLevel()`
- [x] `GET /app/member/levelTable.json` — 클라 로그인 후 1회 캐싱
- [x] 서버 `levelForScore(score)` 헬퍼 — 클라 `getLevel()` 과 동일 결과
- [ ] 레벨업 시 1 스킬 포인트 자동 지급 — `mutiAddUp.json` 처리 시 레벨 증분분만큼 point 증가 로직 추가 필요

### 1.10 아이템/케릭터 화면 🟡
**현재**: `characters` 테이블 존재
- [ ] 사진기 기능 — 얼굴 이미지 교체 (앨범/카메라 접근)
- [ ] 장착 슬롯 (펜/반지/베리/포션)
- [ ] 장착 슬롯 확장 (기본 1 → 최대 4)
- [ ] 본인 공격력/방어력/레벨/경험치/코인 표시
- [ ] 구매 내역 화면 (`구매내역보기`)

### 1.11 광고 🟡 (2026-04-25 서버 완료; 클라 UI 연결 남음)
- [x] 광고 이미지 다운로드 엔드포인트 `/app/member/newAdImageList.json` — member.js 에서 제공
- [x] 광고 조회 카운트 `/app/member/hitViewCount.json` — view_count 증분
- [x] 광고 보상 코인 적립 — `/app/member/adReward.json` (24h 20회 상한, 회당 5코인, `ad_reward_log` 감사)
- [x] 광고 테이블에 `ad_url` 필드 존재 — 클릭 시 외부 URL 이동
- [ ] 틀린그림 자리에 광고 이미지 삽입 (`imageCut` JSON 내에 광고 영역 식별 필드) — 기획서 `market001.png` 스타일
- [ ] 클라: 광고 배너 렌더링 + 클릭 시 브라우저 intent + `adReward` 호출

### 1.12 친구 랭킹 🟡 (2026-04-25 서버 완료; 클라 UI 확장 남음)
- [x] 서버 `GET /app/member/rankListFriends.json?userId=&fbFriends=csv` — 본인 + Facebook 친구 순위
- [x] 서버 `GET /app/member/rankListTop.json?limit=N` — 전체 top N
- [x] 일 단위 상승/하락 `delta` — `rankings` 테이블의 어제 rank_no 와 비교
- [ ] 클라 `RankActivity.java` 에 친구 필터 토글 UI 추가
- [ ] `rankings` 테이블 일일 스냅샷 크론 (현재는 실시간 score 기반 응답)
- [ ] Facebook 친구 목록을 클라에서 전달 (SDK 연동 필요 — 1.3 후속)
- [ ] 순위/캐릭터 이미지/아이디/레벨/경험치 열 UI 구현

### 1.13 컨텐츠 관리 🟡 (2026-04-25 어드민 API 완료; 프론트엔드 UI 남음)
- [x] 어드민 API 베이스 ([admin.js](../server/src/routes/admin.js)) + `X-Admin-Token` 헤더 인증 (`admin_tokens` 테이블)
- [x] 이미지 CRUD `/admin/images` — 틀린그림 + 7개 좌표(imageCut JSON) + view 플래그
- [x] 광고 CRUD `/admin/ads` — ad_image/ad_name/ad_url/expire_date
- [x] 공지 CRUD `/admin/notices`
- [x] 통계 `/admin/stats` — 회원수, 활성 이미지/광고, IAP 검증 수, 24h 광고 보상 요약
- [x] 개발용 기본 토큰 `dev-admin-token-change-me` (운영 배포 시 반드시 재발급)
- [ ] 웹 프론트엔드 (관리자 대시보드 React/HTML) — 별도 작업
- [ ] 이미지 업로드 경로 (현재 URL 만 받음; Cloud Storage 업로드 엔드포인트 추가 필요)
- [ ] 컨텐츠별 조회/플레이 카운트 (현재 `images.view_count` 만, 플레이 카운트는 미구현)

---

## 2. 2순위 🟢 (2026-04-25 전체 구현 완료)

- [x] **Google Analytics / 커스텀 통계 페이지** — Google Analytics 대체용 서버측 이벤트 수집 ([event_log](../server/sql/schema.sql))
  - [x] `POST /app/member/event.json` — 클라가 eventName+payload 로 보고
  - [x] `/admin/analytics/overview` — 회원수 / 24h 신규 / DAU(오늘·어제) / WAU(7일) / 오늘 IAP 건수·코인·보석
  - [x] `/admin/analytics/dau?days=N` — 일별 DAU 추이
  - [x] `/admin/analytics/revenue?days=N` — 스토어·일자별 집계
  - [x] `/admin/analytics/events?hours=N` — 이벤트명 top 50
  - [x] 활동 감지: 이벤트 보고 시 `members.last_active_at` 자동 업데이트
  - [ ] (옵션) Google Analytics 4 연동은 별도 — 현재는 서버 내장 stats 로 대체
- [x] **광고 관리자 페이지 (등록/수정/삭제 + 노출 통계)**
  - [x] 어드민 CRUD 는 1.13 에서 완료 (`/admin/ads`)
  - [x] `ad_images.click_count` 컬럼 + `ad_click_log` 추가
  - [x] `POST /app/member/adClick.json` 클릭 추적
  - [x] `/admin/analytics/ads` — 노출/클릭/CTR %
- [x] **관리자 대시보드 (회원/결제/랭킹/공지)**
  - [x] `GET /admin/ui` — [public/admin.html](../server/public/admin.html) 단일 파일 SPA
  - [x] 탭: 개요 / 통계 / 회원(추후) / 결제 / 스테이지 / 광고 / 공지 / 푸시
  - [x] X-Admin-Token 은 localStorage 보관, 페이지 새로고침 불필요
  - [x] fetch 기반 → 서버 어드민 API 호출, HTML 은 공개 (토큰 없으면 API 만 거부)
- [x] **푸시 알림 (FCM) — 친구 초대 수신 시**
  - [x] `members.fcm_token` + `push_log` 테이블
  - [x] `POST /app/member/registerFcmToken.json` — 클라가 FCM 토큰 등록
  - [x] 어드민 `/admin/push/user/:id` 와 `/admin/push/broadcast`
  - [x] 소켓 107(초대) 오프라인 타겟 자동 푸시 ([socket/server.js onInvite](../server/src/socket/server.js))
  - [x] Firebase Admin SDK 는 optionalDependency (미설치 환경은 dev-log fallback)
  - [ ] Firebase 프로젝트 생성 + 서비스 계정 JSON 발급 (운영 시)
  - [ ] 클라 Android 측 FCM SDK 통합 + 토큰 서버 전송
- [x] **봇 방지 (reCAPTCHA) — 가입 시** ([recaptcha.js](../server/src/util/recaptcha.js))
  - [x] 미들웨어 `recaptcha.middleware` → `/join.json`, `/guest.json` 적용
  - [x] `RECAPTCHA_SECRET` 환경변수 미설정 시 no-op (개발 편의)
  - [x] v3 score 하한 `RECAPTCHA_MIN_SCORE` (기본 0.5)
  - [ ] reCAPTCHA 사이트 키 발급 + 클라 통합 (운영)
- [x] **부정결제 차단 (서버 영수증 재검증)** ([iap.js](../server/src/util/iap.js) + [routes/iap.js](../server/src/routes/iap.js))
  - [x] `POST /app/member/verifyIap.json` — Google Play + Samsung
  - [x] `iap_receipts.purchase_token` UNIQUE 인덱스 → 같은 토큰 재사용 차단
  - [x] verified=true 시에만 wallet 재화 지급 (트랜잭션)
  - [x] 상품 카탈로그 (Items.java GOLD 배열 + Samsung 구 SKU 000001005358~61 호환)
  - [x] googleapis/firebase-admin 은 optionalDependency
  - [ ] Google Play Developer API 서비스 계정 설정 (운영)
  - [ ] Samsung Checkout API 키 발급 (운영)

---

## 3. 현재 코드의 기술부채 (빌드 관련)

기획과 무관하게, `android_project/README.md` 에 명시된 디컴파일 아티팩트:
- [ ] `r0/r5/r13 → this.mTimeCount` 휴리스틱 치환 검증 (애니메이션 속도 오차 가능)
- [ ] `TimerService.java:59` varargs 경고 제거
- [ ] 구형 `WebSettings.setAppCacheEnabled` API 33+ 대응 완료 확인
- [ ] `IRemoteInapService.aidl` 스텁의 실제 바인딩 필요 여부 (KT Olleh 서비스 종료됨 → 스텁으로 유지 OK)
- [ ] IAP 통합 중 KT Olleh / SKT T-Store **제거 검토** (서비스 종료)
  - [ ] `InventoryActivity_Olleh.java` / `InventoryActivity_T.java` / `iap/util/*T.java` 제거
  - [ ] `AndroidManifest.xml` 의 `SmsReceiver` / `TimerService` 제거
  - [ ] Google + Samsung 2트랙으로 단순화

---

## 4. 한글 → 영문 번역 (국제화) 🟢 (2026-04-25 완료)

`번역할 한국어.txt` (135줄) 모두 번역 완료. 영/한 두 리소스가 동일 145개 키 셋으로 정렬됨 (`diff` 무차이).
- [x] 로그인/회원가입 알림 메시지 (ID 형식, 비밀번호 길이, 중복 등) — `notice_msg_*` 25 개
- [x] 대기실 / 대전방 UI 문자열 (`waitingroom`, `makebattleroom`, `friendid`, `invite`, `list`, `wait`, `startgame`, `number`, `id` 등)
- [x] 구매 관련 알림 (`buyitem`, `buyitem1`, `lackcoin`, `alreadybuy`, `buy`, `breakdown`, `deleteitem`)
- [x] 스킬/아이템 이름과 설명 — pen/ring/berry/potion/etc/gold + skill_name_0~7 + skill_text_0~7 모두 양방향 매핑
- [x] [res/values-ko/strings.xml](../android_project/app/src/main/res/values-ko/strings.xml) 생성 (한글 리소스, 145 entry)
- [x] [res/values/strings.xml](../android_project/app/src/main/res/values/strings.xml) 영문 완성 — 누락분 4개 추가(`rank`, `score`, `payment_complete`, `view_more`), 은/금 반지 설명 오역 수정 (increases→reduces)
- [x] 코드 내 한글 하드코딩 제거 — `InventoryActivity_Google/SAMSUNG.java` 의 `Toast "결제 완료"` → `R.string.payment_complete`, 구매내역 더 보기 행 → `R.string.view_more`
- [x] 레이아웃 한글 하드코딩 제거 — `rank_user.xml`, `rank_listrow.xml` (아이디/점수/순위) → `@string/id`, `@string/score`, `@string/rank`
- [ ] (후속) KT Olleh / SKT T-Store 모듈 내 한글은 모듈 자체가 §3 에서 제거 예정이라 손대지 않음
- [ ] (후속) 자바 소스 `LOG.debug("…한글…")` 디버그 문자열은 사용자 노출 없으므로 제외

---

## 5. 이미지 컨텐츠 제작 🟡

`findit_thebattle_contents_guide_20120704/` 참조
- [ ] `market001.png` 포맷 준수: 1개 원본 + 7개 변형 = 8 PNG/세트
- [ ] 각 이미지당 7개 틀린 부위 좌표 정의 (`좌표(x,y)`, `가로세로사이즈`)
- [ ] 광고용 이미지는 광고 문구 + URL 메타데이터 포함
- [ ] 제작 가이드 PDF (`findit_thebattle_contents_guide_20120621.pdf`) 제작자에게 전달

---

## 6. 권장 작업 순서 (제안)

1. ~~**서버 재구축** (Cloud Run + Cloud SQL, GCP_DEPLOYMENT.md)~~ ✅ 코드 완료 (2026-04-25)
2. ~~**클라이언트 서버 주소 교체 + HTTPS 전환**~~ ✅ 완료 (2026-04-25)
3. ~~**재화/아이템 2013 스펙으로 맞추기**~~ ✅ 완료 (2026-04-25)
4. ~~**보석(Gem) 필드 추가**~~ ✅ 완료 (2026-04-25)
5. ~~**레벨/점수/HP 테이블 적재**~~ ✅ 완료 (2026-04-25, 서버 + 클라 테이블 상수)
6. ~~**스킬 트리 44개 확장**~~ ✅ 서버 데이터 완료, 클라 애니메이션 에셋 그룹 매핑 남음
7. ~~**아이템 업그레이드 시스템**~~ ✅ 완료 (2026-04-25)
8. ~~**인벤토리/스킬덱 확장 로직**~~ ✅ 완료 (2026-04-25)
9. **Facebook SDK 클라 통합** (Android 측) — 1.3 후속
10. **KT/SKT IAP 제거 → Google/Samsung 단순화** — 1.8 후속
11. ~~**컨텐츠/광고 관리자 어드민 (API)**~~ ✅ 완료 (2026-04-25), 프론트엔드 UI 별도
12. ~~**번역 및 국제화** (`번역할 한국어.txt` 135줄)~~ ✅ 완료 (2026-04-25, values/values-ko 양방향 145키)
13. **통계/모니터링** + GCP 배포 (최종 단계)

---

## 7. 빠른 레퍼런스

### 주요 상수 (코드 기준)
- [GameView.java:1876](../android_project/app/src/main/java/com/findit/battle/GameView.java#L1876) `GAMETIME = 40` 초
- [Objects.java:58](../android_project/app/src/main/java/com/findit/battle/Objects.java#L58) `FINDRECT_TOTALNUM = 10` (🔴 기획 7개와 불일치)
- [Objects.java:56](../android_project/app/src/main/java/com/findit/battle/Objects.java#L56) `FINDIMAGE_TOTALNUM = 1000`
- [Objects.java:61](../android_project/app/src/main/java/com/findit/battle/Objects.java#L61) `SKILL_TOTALNUM = 50` (배열 크기, 실제 정의된 것은 8개)
- [Items.java:10](../android_project/app/src/main/java/com/findit/battle/Items.java#L10) `TYPEITEM_TOTALNUM = 6`
- [Items.java:24](../android_project/app/src/main/java/com/findit/battle/Items.java#L24) `USERITEM_TOTALNUM = 100`
- [DBAdapter.java:93](../android_project/app/src/main/java/com/findit/battle/DBAdapter.java#L93) 초기 코인 10, 포인트 2

### 기획서 위치
- 최종 아이템/스킬/상점: `findit_item,skill,shop_20130702.xlsx`
- 최종 상점 가격: `findit_shop_items_20121220.xlsx`
- 레벨 점수/HP: `레벨별 첨수체계_기본능력치_02.xlsx`
- AI 공식: `AI_틀린부위 찾는 시간공식_01.xlsx`
- 최종 스킬트리 + 장갑/펜/반지/베리/포션: `스킬트리_04.xlsx`
- 용어 정의: `용어정리.docx`
- 우선순위: `작업 우선순위 v.0.5.docx`
- 컨텐츠 포맷: `findit_thebattle_contents_guide_20120704/contents/findit_틀린그림가이드_20120704.xls`
