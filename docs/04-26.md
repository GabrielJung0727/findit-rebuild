# FindIt: Rebuild — iOS + Android 크로스플랫폼 도입 체크리스트

> 작성일: 2026-04-26
> 전략: **Flutter 단일 코드베이스로 iOS + Android 동시 출시**. 서버(`server/`) 는 그대로 재사용. 디컴파일된 `android_project/` 는 게임 룰·공식의 reference 자료로만 활용 (정식 클라는 `client_flutter/` 가 됨).
> 선행 작업: [docs/04-24.md](04-24.md) (서버 인프라 + 1순위/2순위 + i18n + 컨텐츠 가이드 완료)

---

## 📌 진행 로그

### 2026-04-26 — §8 인벤토리/상점/스킬트리 완료
- 카탈로그: `lib/game/item_catalog.dart` (Items.java 1:1, 2013 spec — 6 카테고리 const)
- 상태: AuthState 가 items[]/skills{}/inventorySize/skillDeckCount 모두 보유, login 응답 자동 흡수 + applyXxx 액션
- InventoryScreen: 6 탭 + 그리드 + 빈 슬롯 + bottom sheet(사용/강화) + Flux picker + 인벤·덱 확장
- ShopScreen: 5 일반 + COIN 분리 탭, buyItemAndSpendCoin 와이어, 코인팩은 §10 위임 placeholder
- SkillTreeScreen: skillCatalog → 5계열 × 9 tier, learned/learnable/locked 시각화, 학습 다이얼로그 + applySkillLearned
- Lobby 하단 IconButton 3개 (인벤/상점/스킬) 라우팅
- 테스트 22 추가 (catalog 16 + auth_deltas 6) — 96/96 통과
- 검증: `flutter analyze` 0, debug APK 12.8s 빌드

### 2026-04-26 — §7 게임 루프 (GameView 포팅) 완료
- 모델: `image_set.dart` (ImageCut + ImageSet + parseImageCutJson), `game_state.dart` (PlayerState/GameState/GameResult/GameEndReason)
- 컨트롤러: `game_controller.dart` — Timer.periodic(16ms) ticker, AI 자동 발견(공식 `7-level×0.03` ±15%), 정답/오답 hit test, 종료 4가지 사유, WS 106 송수신 + 102 leave 처리
- 렌더링: `game_painter.dart` (자기/상대 발견 영역 + 글로우 펄스 + 오답 X 플래시) + `game_view.dart` (CachedNetworkImage 디코드 + 픽셀 좌표 변환 + 햅틱)
- 화면: `game_screen.dart` (HUD: 진행률·콤보 배지·HP·40s 카운트다운) + `result_screen.dart` (승/패 + 보상 + 종료 사유)
- 라우팅: `/game`, `/result` 추가. LobbyScreen "Play vs AI" 진입, BattleRoom bothReady 진입 모두 wired
- 게임 종료 시 서버 `mutiAddUp.json` 호출 (fire-and-forget)
- 테스트: 14 추가 (image_set 9 + constants 5 + game_controller 5) — 88/88 통과
- 검증: `flutter analyze` 0, `flutter test` 88/88, debug APK 27.3s 빌드
- 후속: 스킬 애니메이션 정식 에셋 (§13), HP 데미지 룰 (기획 미정), mutual image id 라우팅 (멀티 동기화)

### 2026-04-26 — §6 대기실/대전방 + §12 WebSocket 마이그레이션 (대부분) 완료
- 서버: `socket/handlers.js` 신설로 TCP/WS 공유 핸들러 + 상태. `socket/server.js` (TCP) 슬림화, `socket/ws_server.js` 추가 (ws 패키지, `/ws` upgrade). 같은 process state 공유 → 두 클라가 같은 방 매칭 가능.
- 클라: `lib/api/ws_client.dart` (지수 backoff 재연결 + 앱 lifecycle 통합 + `WsFrame` 파싱), `lib/state/lobby.dart` (Riverpod, 7 코드 라우팅, 5/8 스킬덱 락)
- 화면: WaitingRoomScreen (유저 목록 + 방만들기 + ID 초대 + 자동 초대 다이얼로그) + BattleRoomScreen (좌·우 캐릭터 + 5/8 스킬 picker + 준비 토글)
- Router: `/lobby` → `/waiting` → `/battle` 자동 전이, leaveRoom 시 복귀
- 테스트: WS roundtrip (실 dart:io 서버) + 5개 lobby 시나리오 (fake echo) — 65/65 통과
- 검증: `flutter analyze` 0, `flutter test` 65/65, debug APK 11.2s 빌드
- 후속: 인증/heartbeat (§12), Facebook 친구 목록 탭 (1.3 후속)

### 2026-04-26 — §5 로그인/회원가입/Guest 화면 + 라우터 완료
- 신규 서버 엔드포인트: `POST /app/member/checkUserId.json` (userId+userNick 사전 중복 검증)
- `lib/util/validation.dart` — 서버 `validation.js` 규칙 1:1 포팅
- `lib/state/auth.dart` — Riverpod `StateNotifierProvider` 로 인증 상태, SharedPreferences 자동 로그인 영속, `tryAutoLogin()` 부팅 시 silent
- `go_router` + 인증 redirect (`/login` ↔ `/lobby`)
- LoginScreen / JoinScreen / GuestScreen / LobbyScreen 신규 + login_messages.dart (서버 result 코드 → 번역 매퍼)
- JoinScreen 의 "중복확인" 버튼 + 즉시 helperText 피드백 + 가입 성공 시 자동 로그인 + 라우터가 lobby 로 이동
- Facebook 버튼은 `Property.facebookAppId` 비어있으면 숨김, reCAPTCHA 토큰 슬롯은 TODO 코멘트로 예약
- 테스트 50/50 통과 (validation 12 + auth 6 + api 32), `flutter analyze` 0, debug APK 28.9s 빌드 성공

### 2026-04-26 — §4 REST API 클라이언트 27 엔드포인트 + 30 테스트 완료
- 8 모듈 분할: api_client / member / shop / catalog / content / game / analytics / iap / push
- 공유 헬퍼: `unwrapResult()` (result=='000' → throw `ApiResultException`), 401 핸들러 콜백, secure_storage 토큰 자동 첨부
- Riverpod `providers.dart` 8개 provider (apiClient 공유) 와이어
- `flutter_html` 런타임 + `http_mock_adapter` dev dep 추가
- 테스트 fixture: in-memory FlutterSecureStorage stub + `UrlRequestMatcher` (URL+method 매칭, body 무시)
- 검증: `flutter analyze` 0 issues, `flutter test` 30/30 통과, `flutter build apk --debug` 9.9s 통과

### 2026-04-26 — §2 네트워크 보안 + §3 i18n 145 키 이식 완료
- **§2**: `lib/util/property.dart` 안드 `Property.java` 1:1 매핑 (deprecated 표기 포함), iOS Info.plist `NSAllowsArbitraryLoads=false` + `NSAllowsLocalNetworking=true` (private/loopback 만 평문), Android `network_security_config.xml` 안드 원본 그대로 이식 + Manifest 결합 + INTERNET 권한
- **§3**: `client_flutter/tools/strings_xml_to_arb.js` (Node, idempotent) — 145키 안드 strings.xml → camelCase ARB 자동 변환, parity 100% (only-en/only-ko=0), CRLF→LF 정규화, `flutter gen-l10n` 으로 3개 dart 파일 각 145 getter 생성
- 검증: `flutter analyze` 0 issues, `flutter test` 통과, `flutter build apk --debug` 통과
- 남은 항목: iOS Pod (§15 macOS CI 시), 디바이스 시각 스모크 (§15 함께)

### 2026-04-26 — §1 Flutter 부트스트랩 완료
- Flutter 3.27.4 stable SDK 다운로드 + 설치 (`C:\src\flutter`)
- `client_flutter/` 신규 프로젝트 — org `com.findit`, applicationId `com.findit.battle`, ios+android 두 플랫폼
- pubspec.yaml: 11개 핵심 의존성 (dio/ws/riverpod/intl/secure_storage/sharedpref/cached_image/firebase/iap/facebook) + flutter_launcher_icons
- AGP 8.1→8.5, Gradle 8.3→8.7, Kotlin 1.8.22→1.9.24, JVM 1.8→17, compileSdk 36, minSdk 21 (JDK 21+ 차단 이슈 해소)
- 디렉터리 구조 + W1 시드 코드: Property/GameConstants/SocketCode 상수, Dio ApiClient, MemberApi (join/login/guest/logout/levelTable/skillCatalog), Riverpod providers, l10n 파이프라인 (영/한 ARB)
- analysis_options 강화 (strict-casts/inference, prefer_single_quotes 등) → `flutter analyze` 0 issues
- `flutter test` 통과, `flutter build apk --debug` 통과 (22.7s) — debug APK 생성 확인
- iOS Pod 검증은 Windows 에서 불가, §15 macOS CI 셋업 시 수반 예정

### 2026-04-26 — 초기 작성
- 방향성 결정: Flutter (네이티브 2트랙·PWA·Unity 비교 후 채택)
- 서버는 무수정 — 단, 매칭 채널은 raw TCP `:22131` → **WebSocket** 마이그레이션 (iOS 백그라운드 호환성)
- 디컴파일 `android_project/` 는 게임 공식·HP 테이블·아이템 가격 등 진실 자료로만 사용
- 본 문서는 **신규 클라 구축** 트랙 — 기존 04-24.md 의 서버/문서 작업과는 독립적으로 병행 가능

---

## 0. 전제 / 선결정 사항

| 항목 | 값 | 비고 |
|---|---|---|
| 프레임워크 | Flutter 3.24+ | Dart 3.5+ |
| 최소 OS | iOS 14 / Android API 21 (Lollipop) | 대상 디바이스 점유율 ≥ 95% |
| Bundle ID | `com.findit.battle` (제안) | 안드 패키지명 동일 유지 → IAP/푸시 설정 단순화 |
| 상태 관리 | Riverpod 2.x | provider 보다 testable, GetIt 별도 불필요 |
| HTTP | dio + retrofit | 인터셉터 (세션 토큰 자동 첨부) 용이 |
| 실시간 | web_socket_channel | dart:io WebSocket wrapper |
| i18n | flutter_localizations + intl + ARB | 기존 `strings.xml` → ARB 자동 변환 |
| 빌드 자동화 | Codemagic 또는 Fastlane | TestFlight + Play Internal Track |

### 차단 요소 / 사전 발급 필요
- [ ] Apple Developer 계정 (\$99/year) — TestFlight·App Store 출시 필수
- [ ] Apple APNs 푸시 인증서 (or Auth Key)
- [ ] App Store Connect 앱 등록 + Bundle ID 등록
- [ ] Google Play Developer 계정 (\$25 one-time, 이미 있을 가능성)
- [ ] Firebase 프로젝트 (Android + iOS 양쪽 앱 등록)
- [ ] StoreKit (Apple) 상품 SKU 4종 등록 — 100/250/500/650 코인팩
- [ ] CDN 결정 — GCS / CloudFront / 기타 (이미지 자산 호스팅)

---

## 1. Flutter 부트스트랩 🟢 (2026-04-26 완료 — iOS Pod / 실아이콘 자산만 후속)

- [x] **Flutter SDK 설치** — 3.27.4 stable @ `C:\src\flutter` (Dart 3.6.2). `flutter doctor`: Android toolchain·Studio·VS·Chrome 모두 정상.
- [x] **프로젝트 생성** — [`client_flutter/`](../client_flutter/) — `flutter create --org com.findit --project-name findit --platforms=ios,android`
- [x] **Bundle/Package**: `applicationId = "com.findit.battle"` ([android/app/build.gradle](../client_flutter/android/app/build.gradle)), Kotlin 패키지 디렉터리 `kotlin/com/findit/battle/MainActivity.kt` 로 이동, Manifest `android:label="Findit"`
- [x] **`pubspec.yaml`** ([client_flutter/pubspec.yaml](../client_flutter/pubspec.yaml)) 핵심 의존성 11종 모두 추가, `flutter pub get` 으로 82 packages resolved:
  - [x] `dio: ^5.7.0` (REST)
  - [x] `web_socket_channel: ^3.0.1` (WebSocket)
  - [x] `flutter_riverpod: ^2.6.1` (상태 관리)
  - [x] `flutter_localizations` (SDK) + `intl: ^0.19.0`
  - [x] `flutter_secure_storage: ^9.2.2` (세션 토큰 — Keychain/Keystore)
  - [x] `shared_preferences: ^2.3.3`
  - [x] `cached_network_image: ^3.4.1`
  - [x] `firebase_core: ^3.8.0` + `firebase_messaging: ^15.1.5`
  - [x] `in_app_purchase: ^3.2.1`
  - [x] `flutter_facebook_auth: ^7.1.1`
- [x] **디렉터리 구조** — `lib/{api,models,state,screens,game,l10n,util}/` + `assets/{images,icon}/` 모두 신설
- [x] **W1 시드 코드**:
  - [x] [lib/util/property.dart](../client_flutter/lib/util/property.dart) — `Property.serverDomain` / `webSocketUrl` (`--dart-define` 오버라이드)
  - [x] [lib/util/constants.dart](../client_flutter/lib/util/constants.dart) — `GameConstants` (gameTime 40, findRectTotalNum 7, AI 시간 공식, 콤보 보너스, 점수 공식) + `EconomyConstants` + `SocketCode`
  - [x] [lib/api/api_client.dart](../client_flutter/lib/api/api_client.dart) — Dio 베이스 + 세션 토큰 인터셉터
  - [x] [lib/api/member_api.dart](../client_flutter/lib/api/member_api.dart) — `join`/`login`/`guest`/`logout`/`levelTable`/`skillCatalog` (W2 에서 확장)
  - [x] [lib/state/providers.dart](../client_flutter/lib/state/providers.dart) — Riverpod `apiClientProvider` / `memberApiProvider`
  - [x] [lib/screens/login_screen.dart](../client_flutter/lib/screens/login_screen.dart) — placeholder
  - [x] [lib/main.dart](../client_flutter/lib/main.dart) — `ProviderScope` + `MaterialApp` + l10n delegates
- [x] **i18n 파이프라인** — [l10n.yaml](../client_flutter/l10n.yaml) + [lib/l10n/app_en.arb](../client_flutter/lib/l10n/app_en.arb) / [app_ko.arb](../client_flutter/lib/l10n/app_ko.arb), `flutter gen-l10n` 으로 `app_localizations*.dart` 3종 자동 생성. 145키 전량 변환은 §3 작업.
- [x] **`analysis_options.yaml`** ([client_flutter/analysis_options.yaml](../client_flutter/analysis_options.yaml)) — `flutter_lints` + `strict-casts/inference/raw-types` + `prefer_single_quotes`/`require_trailing_commas`/`avoid_print`/`avoid_dynamic_calls`/`use_super_parameters`. `lib/l10n/*` 자동생성 파일 exclude.
- [x] **`flutter analyze` 0 issues** ✅ — `lib/main.dart`, `lib/screens/login_screen.dart` `!` non-null assertion 제거 + Dio 제네릭 인퍼런스 명시화
- [x] **`flutter test` 통과** — `widget_test.dart` 가 `FinditApp` 빌드 + `Findit` 텍스트 검증
- [x] **Android Gradle 빌드 통과** — `flutter build apk --debug` 22.7s (캐시 후), APK at [`client_flutter/build/app/outputs/flutter-apk/app-debug.apk`](../client_flutter/build/app/outputs/flutter-apk/app-debug.apk)
  - AGP 8.1.0 → **8.5.0**, Gradle 8.3 → **8.7**, Kotlin 1.8.22 → **1.9.24** 업그레이드 (JDK 21+ 호환성 — 04-26.md §1 부트스트랩 시 발견된 차단 이슈 해소)
  - Java/JVM compatibility 1.8 → **17**, `compileSdk = 36` (flutter_facebook_auth 요구), `minSdk = 21`
- [x] **`flutter_launcher_icons` 설정** — pubspec.yaml 에 config 작성 (Android+iOS, min_sdk 21, ios 알파 제거). 실제 `assets/icon/icon.png` 자산은 후속 (§13 자산 / 디자인 입수 시).
- [ ] **iOS Pod 환경** (`cd ios && pod install`) — Windows 환경 검증 불가, **macOS 머신 또는 CI 에서 1회 동작 확인 필요** (§15 CI/CD 의 Codemagic/Fastlane 셋업 시 자동 수반)
- [ ] **임시 앱 아이콘 PNG 자산 배치** — 1024×1024 placeholder 제작 후 `assets/icon/icon.png` 두면 `flutter pub run flutter_launcher_icons` 으로 양 플랫폼 아이콘 자동 생성

---

## 2. 서버 주소 / 네트워크 보안 🟢 (2026-04-26 완료)

- [x] [lib/util/property.dart](../client_flutter/lib/util/property.dart) — Android `Property.java` 1:1 매핑
  - [x] `serverDomain` — `--dart-define=FINDIT_SERVER_DOMAIN`, 기본 `http://10.0.2.2:8080/`
  - [x] `webSocketUrl` — 기본 `ws://10.0.2.2:22131/ws` (운영 시 `wss://...`)
  - [x] `serverIp` / `serverSocketPort` — `@Deprecated` 표기, BC 만 유지
  - [x] `usesCleartextTraffic` — 운영 빌드 false 토글 지점
  - [x] 외부 서비스 ID — `facebookAppId`, `recaptchaSiteKey`, `adhubId`, Samsung IAP SKU 4종
- [x] **iOS `Info.plist`** ([client_flutter/ios/Runner/Info.plist](../client_flutter/ios/Runner/Info.plist))
  - [x] `NSAllowsArbitraryLoads = false` (App Store 통과 조건)
  - [x] `NSAllowsLocalNetworking = true` — 10.0.0.0/8, 172.16/12, 192.168/16, 169.254/16, 127/8, *.local 평문 허용 (시뮬·LAN 실기기 디버깅 커버)
  - [x] 운영 빌드 시 `NSAllowsLocalNetworking` 도 false 로 토글 — 코멘트 명시
- [x] **Android `network_security_config.xml`** ([client_flutter/.../res/xml/network_security_config.xml](../client_flutter/android/app/src/main/res/xml/network_security_config.xml))
  - [x] 기존 `android_project/` 정책 그대로 이식 — base-config `cleartextTrafficPermitted=false` + 5개 도메인 예외 (10.0.2.2 / 127.0.0.1 / localhost / 192.168.0.0 / 192.168.1.0)
  - [x] AndroidManifest 에 `android:usesCleartextTraffic="false"` + `android:networkSecurityConfig="@xml/network_security_config"` 결합
  - [x] `INTERNET` / `ACCESS_NETWORK_STATE` 권한 추가
- [x] Build sanity: `flutter analyze` 0 issues, `flutter test` 통과, `flutter build apk --debug` 통과 (12.5s 캐시)

---

## 3. i18n — 기존 strings.xml 이식 🟢 (2026-04-26 완료, 디바이스 시각 검증만 후속)

- [x] [client_flutter/tools/strings_xml_to_arb.js](../client_flutter/tools/strings_xml_to_arb.js) — Node 스크립트, idempotent
  - [x] 입력: `android_project/.../res/values/strings.xml` + `values-ko/strings.xml`
  - [x] 출력: `client_flutter/lib/l10n/app_en.arb` + `app_ko.arb`
  - [x] 변환: snake_case → camelCase 자동 (`item_pen01` → `itemPen01`), `\n`/`\t`/escape/따옴표/CRLF→LF 정규화
  - [x] **Parity 검증**: 145 키 양방향 일치, only-en/only-ko 둘 다 0
  - [x] camelCase 변환 후 충돌 검사 — 없음
  - [x] 멱등성 — 같은 입력 → 같은 출력 (재실행 무차이)
- [x] **`flutter gen-l10n` 정상 동작** — [l10n.yaml](../client_flutter/l10n.yaml) 기준으로 `app_localizations.dart` + `app_localizations_en.dart` + `app_localizations_ko.dart` 3종 자동 생성, **각 145개 getter** 확인
- [x] **`MaterialApp` localizations 와이어** — [lib/main.dart](../client_flutter/lib/main.dart) 에 `localizationsDelegates: AppLocalizations.localizationsDelegates`, `supportedLocales: AppLocalizations.supportedLocales`
- [x] **iOS `Info.plist CFBundleLocalizations`** — `[en, ko]` 배열 추가
- [x] Build sanity: `flutter analyze` 0 issues, `flutter test` 통과, debug APK 빌드 통과
- [ ] **디바이스 언어 한/영 토글 시각 검증 (스모크)** — 에뮬/실기기 부팅 후 시스템 언어 토글로 확인. §15 (CI/CD 셋업) 와 함께 진행 시 자동 수반.

---

## 4. REST API 클라이언트 🟢 (2026-04-26 완료 — 27 엔드포인트, 8 모듈, 30 테스트)

서버 엔드포인트 27개 ([server/src/routes/](../server/src/routes/)) 1:1 wrapper. `lib/api/` 8 모듈로 분할.

- [x] **[lib/api/api_client.dart](../client_flutter/lib/api/api_client.dart)** — Dio 인스턴스 + baseUrl + 인터셉터
  - [x] 세션 토큰 자동 첨부 (`Authorization: Bearer <token>`) — `flutter_secure_storage` Keychain/Keystore
  - [x] **401 핸들러**: 토큰 폐기 + `registerUnauthorizedHandler(callback)` 호출 → 라우터가 로그인 화면 복귀
  - [x] **공유 헬퍼**: `unwrapResult()` (result=='000' 검증 + throw `ApiResultException`), `asMap()` (raw map 캐스팅), `ResultCode` 상수
- [x] **[member_api.dart](../client_flutter/lib/api/member_api.dart)** — 인증 + 지갑
  - [x] `POST join.json` (recaptcha 옵션) / `login.json` / `guest.json` / `facebook.json` / `logout.json` — 응답 `token` 자동 저장
  - [x] `POST addCoin.json` / `spendCoin.json` / `spendGem.json` / `addGem.json`
- [x] **[shop_api.dart](../client_flutter/lib/api/shop_api.dart)** — 구매/사용/학습/강화/확장
  - [x] `POST buyItemAndSpendCoin.json` / `spendMyItem.json` / `buySkillAndSpendPoint.json` / `learnSkill.json` / `upgradeItem.json` (fluxUsed) / `expandInventory.json` / `expandSkillDeck.json`
- [x] **[catalog_api.dart](../client_flutter/lib/api/catalog_api.dart)** — 정적 메타데이터
  - [x] `GET skillCatalog.json` (44 스킬, requiredSkillIds CSV → int[]) / `levelTable.json` (101 레벨)
- [x] **[content_api.dart](../client_flutter/lib/api/content_api.dart)** — 스테이지/광고 이미지 + 공지
  - [x] `GET newImageList.json` (`imgId` since-cursor, imageCut JSON 그대로 전달, adCaption 포함) / `newAdImageList.json` / `notice.json` (HTML, `flutter_html` 패키지로 렌더)
- [x] **[game_api.dart](../client_flutter/lib/api/game_api.dart)** — 게임 결과 + 랭킹 + 구매내역
  - [x] `POST mutiAddUp.json` (level GREATEST + 점수/코인/포인트 증분) / `GET rankList` (top 100, 응답 `result` 없음) / `rankListFriends.json` (Facebook 친구 + delta) / `rankListTop.json` / `itemPurchaseHistList`
- [x] **[analytics_api.dart](../client_flutter/lib/api/analytics_api.dart)** — 이벤트 + 광고 추적
  - [x] `POST event.json` (fire-and-forget) / `adClick.json` / `adReward.json` (24h 20회 상한 → throw `ApiResultException` 'daily_cap_reached') / `hitViewCount.json`
- [x] **[iap_api.dart](../client_flutter/lib/api/iap_api.dart)** — 영수증 검증
  - [x] `POST verifyIap.json` — `'google'` / `'samsung'` 지원. **Apple 추가는 §10 — 서버 enum + iap.js productMap + Apple App Store Server API 와 동시 작업**
- [x] **[push_api.dart](../client_flutter/lib/api/push_api.dart)** — `POST registerFcmToken.json`
- [x] **[lib/state/providers.dart](../client_flutter/lib/state/providers.dart)** — 8개 Riverpod `Provider` (apiClient + 7 wrapper) — 단일 ApiClient 공유
- [x] **단위 테스트 — 30개** ([test/api/](../client_flutter/test/api/))
  - [x] `_helper.dart` — `TestApi` fixture: in-memory FlutterSecureStorage stub + DioAdapter (`UrlRequestMatcher`, URL+method 기반 매칭)
  - [x] 각 모듈별 테스트 1~6개. URL 라우팅 + 응답 파싱 + 토큰 영속화 + ApiResultException throw 모두 커버
  - [x] `flutter test` 30/30 통과 ✅
- [x] **빌드 검증** — `flutter analyze` 0 issues, `flutter build apk --debug` 9.9s 통과

---

## 5. 화면 — 로그인 / 회원가입 / Guest 🟢 (2026-04-26 완료, FB SDK / reCAPTCHA 실 토큰만 후속)

원본: `LoginActivity` 다이얼로그(`GameActivity.mLoginDlg`) + `JoinActivity.java`(508줄)

- [x] **[lib/util/validation.dart](../client_flutter/lib/util/validation.dart)** — 서버 [validation.js](../server/src/util/validation.js) 규칙 1:1 (이메일 정규식, 비번 4~12자, 닉네임 1~20자 trim, 캐릭터 0~2)
- [x] **[lib/state/auth.dart](../client_flutter/lib/state/auth.dart)** — `AuthUser` / `AuthState` / `AuthController` (Riverpod `StateNotifierProvider`)
  - login/guest/logout 액션 + `tryAutoLogin()`
  - 자동로그인 토큰: `SharedPreferences` (`auto_login` 플래그 + `saved_user_id`/`saved_password`)
  - Guest 는 자동로그인 흔적 남기지 않음 (정책)
- [x] **[router.dart](../client_flutter/lib/router.dart)** — `go_router` + Riverpod 인증 redirect (비로그인 → `/login`, 로그인 → `/lobby`)
- [x] **[LoginScreen](../client_flutter/lib/screens/login_screen.dart)** — 이메일/비번/자동로그인 토글, 로그인 / 회원가입 / Guest 입장 / Facebook(gated) 버튼, 비번 가시성 토글, 진행 중 dim, error 코드별 다이얼로그
- [x] **[JoinScreen](../client_flutter/lib/screens/join_screen.dart)** — 이메일·닉네임·비번·비번확인 + 캐릭터 0~2 picker + **중복확인** 버튼 → 즉시 helperText 피드백 (`available` / `taken`), 가입 성공 후 자동 로그인 → 라우터가 lobby 로 이동
- [x] **[GuestScreen](../client_flutter/lib/screens/guest_screen.dart)** — 안내 카피 + 입장 버튼 (가입 유도)
- [x] **[LobbyScreen](../client_flutter/lib/screens/lobby_screen.dart)** — placeholder, §6 대기실로 교체 예정. 닉네임/Lv/Score/Coin/Point/Gem/HP 표시 + 로그아웃
- [x] **[login_messages.dart](../client_flutter/lib/screens/login_messages.dart)** — 검증/서버 result 코드 → 번역 메시지 매퍼 (`noticeMsgNoid` 등 145키 활용)
- [x] **서버 신규 엔드포인트** — `POST /app/member/checkUserId.json` ([routes/member.js](../server/src/routes/member.js)) — userId/userNick 둘 중 하나 이상 받아 `{valid, taken, error?}` 반환
- [x] **MemberApi.checkUserId** wrapper + 테스트 추가
- [x] **테스트 — 50개 (이전 30 + 20 신규)**
  - `test/util/validation_test.dart`: 12개 (이메일/비번/닉/캐릭터 경계값)
  - `test/state/auth_test.dart`: 6개 (login/guest/logout 상태 전이 + SharedPreferences 영속화 + tryAutoLogin)
  - `test/api/member_api_test.dart`: checkUserId 추가
- [x] **빌드 검증** — `flutter analyze` 0 issues, `flutter test` 50/50 통과 ✅, `flutter build apk --debug` 28.9s 통과 ✅
- [ ] **Facebook 로그인 실 SDK 통합** — `flutter_facebook_auth` 의존성 + `Property.facebookAppId` 발급 + 안드 `strings.xml` `facebook_app_id` + iOS `Info.plist` `FacebookAppID` (1.3 후속)
- [ ] **reCAPTCHA v3 실 토큰** — `Property.recaptchaSiteKey` 발급 + Enterprise `execute()` 호출 + `joinScreen._submit` 의 `recaptchaToken:` 슬롯 채우기. 현재는 미설정 시 서버 미들웨어가 no-op 이라 개발에는 지장 없음.

---

## 6. 화면 — 대기실 / 대전방 🟢 (2026-04-26 완료, §12 WS 마이그레이션 부분 동시 완료)

원본: `WaitingRoom.java`(415줄), `GameView.BattleRoom`(1224~1870줄). 안드 원본은 raw TCP `:22131` + 4자리 길이 prefix 프레임. Flutter 클라는 **WebSocket** 사용 (§12 부분 완료).

### 서버 — WS 게이트웨이 + 핸들러 분리
- [x] [server/src/socket/handlers.js](../server/src/socket/handlers.js) — TCP/WS 양쪽이 공유하는 공통 핸들러 + 인메모리 상태 (sessions/waiting/rooms). 7개 코드(100/101/102/103/104/106/107) 모두 이전. 메시지 프레임은 트랜스포트 독립 텍스트 (`|code|arg1|...`).
- [x] [server/src/socket/server.js](../server/src/socket/server.js) — TCP 어댑터로 슬림화 (디컴파일 안드 APK 호환용 유지). `Connection` 인터페이스 (`send`/`destroyed`) 로 wrapping.
- [x] [server/src/socket/ws_server.js](../server/src/socket/ws_server.js) — 신규 WebSocket 어댑터, `ws` 패키지 (`optionalDependency`). `attach(httpServer, {path:'/ws'})` 로 Express HTTP 서버에 upgrade 핸들러 부착.
- [x] [server/src/index.js](../server/src/index.js) — `app.listen()` 의 반환 httpServer 에 `wsServer.attach()`. TCP 서버는 병행 가동.
- [x] 같은 프로세스의 handlers 상태를 두 트랜스포트가 공유 → 안드 원본과 Flutter 클라가 같은 방에서 매칭 가능.

### 클라 — WebSocket + Lobby 상태 + 두 화면
- [x] [lib/api/ws_client.dart](../client_flutter/lib/api/ws_client.dart) — `WebSocketChannel` 래퍼
  - `connect()` / `disconnect()` / `dispose()` / `send(code, args)`
  - 자동 재연결 — 지수 backoff (1→2→4→8→16→30s, 최대 5회)
  - `onLifecycleChange(state)` — paused 시 close, resumed 시 reconnect (의도 `_wantConnected` 유지)
  - `messages` broadcast Stream + `WsFrame.parse()` (`|code|args` 파싱, `ok`/`payload` 헬퍼)
- [x] [lib/state/lobby.dart](../client_flutter/lib/state/lobby.dart) — `LobbyController` Riverpod
  - `WaitingState`(users + connected + invitedBy/Room) + `BattleRoomState`(roomName + isCreator + opponent + ready + skillDeck)
  - 7 코드 수신 라우팅 (100/101/102/103/104/107) → 상태 전이
  - 액션: `connect/refreshUserList/createRoom/leaveRoom/enterRoom/setReady/invite/toggleSkill/acceptInvite/declineInvite`
  - 스킬덱 5/8 제한 + selfReady 시 락
- [x] [WaitingRoomScreen](../client_flutter/lib/screens/waiting_room_screen.dart)
  - [x] 접속 유저 목록 (소켓 100 응답)
  - [x] 대전방 만들기 버튼 (소켓 101) + 새로고침
  - [x] 친구 ID 직접 초대 다이얼로그 (소켓 107)
  - [x] 리스트에서 친구 선택 → 즉시 enterRoom
  - [x] 초대 수신 시 자동 다이얼로그 (수락 → 103, 거절 → 무시)
  - [x] 앱 lifecycle (resumed) 시 자동 user list 새로고침
  - [ ] **Facebook 친구 목록 연동 (1.3 후속)** — `flutter_facebook_auth` SDK 연동 후 친구 UID 리스트로 별도 탭
- [x] [BattleRoomScreen](../client_flutter/lib/screens/battle_room_screen.dart)
  - [x] 좌(자신)/우(상대) 캐릭터 카드 + ready 인디케이터
  - [x] 스킬덱 5/8 picker (4×2 grid, 선택/잠금/비활성 시각화)
  - [x] "준비 중" → "기다리기" → "게임하기" 버튼 전환 (selfReady/opponentReady 양쪽 만족 시)
  - [x] 준비 상태 동기화 (104 송수신)
  - [x] 뒤로가기 = leaveRoom (소켓 102)
- [x] [router.dart](../client_flutter/lib/router.dart) — `/lobby` → `/waiting` → `/battle` 트랜지션. battleRoom 진입 시 자동 `go('/battle')`, 종료 시 `go('/waiting')`.
- [x] LobbyScreen 의 "게임하기" 버튼 → `/waiting`

### 테스트 — 6 추가 (총 65)
- [x] [test/api/ws_client_test.dart](../client_flutter/test/api/ws_client_test.dart) — `WsFrame.parse` 3종 + 실 dart:io WebSocket 서버 띄워 connect/send/receive roundtrip 1종
- [x] [test/state/lobby_test.dart](../client_flutter/test/state/lobby_test.dart) — fake WS echo server 띄워 5 시나리오: user list, createRoom, 초대 받기→수락, opponentReady 수신, skillDeck 5/8 제한
- [x] **빌드 검증** — `flutter analyze` 0 issues, `flutter test` 65/65 통과 ✅, `flutter build apk --debug` 11.2s 통과 ✅

---

## 7. 게임 루프 — GameView 포팅 🟢 (2026-04-26 완료, 스킬 애니메이션·HP 데미지 룰만 후속)

원본: `GameView.java` ~3870줄. 핵심 룰/공식만 추려 Dart 로 깔끔히 재작성. 진실 자료는 서버 [`balance.js`](../server/src/util/balance.js) + 클라 [`util/constants.dart`](../client_flutter/lib/util/constants.dart) (이미 §1 에서 동기화).

### 모델
- [x] [lib/game/image_set.dart](../client_flutter/lib/game/image_set.dart) — `ImageCut`(`x,y,w,h` + `contains`/`toRect`) + `ImageSet`(7 cuts + adCaption + url) + `parseImageCutJson()` (JSON string 또는 List 모두 수용)
- [x] [lib/game/game_state.dart](../client_flutter/lib/game/game_state.dart) — `PlayerState`(found×7 + combo + hp/maxHp) + `GameState`(image + self + opponent + timeRemaining + aiNextFind + lastCorrectIndex + lastWrongTouch + result) + `GameResult`(reason + won + selfScore/opponentScore/coinReward/pointReward) + `GameEndReason` enum

### 컨트롤러 + 게임 루프
- [x] [lib/game/game_controller.dart](../client_flutter/lib/game/game_controller.dart) — `StateNotifier<GameState>` + `Timer.periodic(16ms)` ticker
  - [x] **터치 hit test** — `onTap(pixelOffset)` → 7 cuts 순회, 정답이면 `combo++` + 시간 보너스, 오답이면 `combo=0` + `lastWrongTouch` 기록
  - [x] **AI 타이머** — `7 - level × 0.03` 초 ±15% 랜덤 (1.0..7.0 클램프), 만료 시 미발견 인덱스 중 랜덤 선택 → `_opponentFoundIndex` (자기 콤보 0 으로 리셋)
  - [x] **점수 공식** — `GameConstants.calculateScore` (승자 `findNum*50+100+combo` / 패자 `findNum*50+combo`)
  - [x] **콤보 보너스** — 1→100, 2→200, 3→400, 4+→700점 / 시간 1→1, 2→2, 3→4, 4+→7초 ([`GameConstants.combo*`](../client_flutter/lib/util/constants.dart))
  - [x] **HP 시스템** — `PlayerState.maxHp` 가 로그인 응답의 hp (서버 `levelTable.json` 의 `hp` 필드)
  - [x] **게임 시간** — `GAMETIME = 40` 초 카운트다운 (`timeRemaining` 0 → 종료)
  - [x] **상대 발견 시** — `mCombo=0` + `lastCorrectByOpponent` 시각 피드백
  - [x] **종료 조건** — 시간 만료 / selfCleared(7/7) / opponentCleared / opponentLeft. `_finalize` 가 `GameResult` 채움.
  - [x] **WS 106 송수신** — 멀티 모드에서 정답 발견 시 `|106|<char>|found,<idx>` 송신, 수신 시 `_opponentFoundIndex` 호출. `|102|` (leaveRoom) 수신 시 `opponentLeft` 종료.
  - [ ] **HP 데미지 룰** — 어떤 스킬이 HP 를 깎는지 기획서 미정의 (04-24.md §1.4 last bullet). 데미지 모델 결정 후 `_applyDamage` 추가.

### 렌더링
- [x] [lib/game/game_painter.dart](../client_flutter/lib/game/game_painter.dart) — `CustomPainter`
  - [x] 원본 PNG letterbox fit (`paintImage`) + 캔버스 배경
  - [x] 자기 발견 영역 — 초록 반투명 + 테두리
  - [x] 상대 발견 영역 — 주황 반투명 + 테두리 (자기 마크가 우선)
  - [x] 최근 정답 — `lastCorrectIndex` 영역에 글로우 펄스
  - [x] 오답 플래시 — `lastWrongTouch` 위치에 빨간 X (외부 AnimationController 가 `flashIntensity` 0..1 로 페이드)
- [x] [lib/game/game_view.dart](../client_flutter/lib/game/game_view.dart) — 위젯
  - [x] `cached_network_image` 의 `CachedNetworkImageProvider` → `ui.Image` 디코드 (8s 타임아웃)
  - [x] `GestureDetector.onTapUp` → 화면 좌표를 letterbox scale 로 픽셀 좌표 변환 → `widget.onTap(pixel)`
  - [x] `HapticFeedback.mediumImpact()` (정답) / `lightImpact()` (오답)

### 화면
- [x] [lib/screens/game_screen.dart](../client_flutter/lib/screens/game_screen.dart) — Scaffold (검은 배경)
  - [x] 상단 HUD — P1/P2(또는 AI) 진행률 + 콤보 배지 + HP 바 + 중앙 카운트다운 (10초 미만 빨강)
  - [x] 본체 GameView
  - [x] 하단 leave 버튼 + 광고문구 표시 (`adCaption`)
  - [x] 게임 종료 listen → 서버 `mutiAddUp.json` 호출 (fire-and-forget) → `/result` 라우트 이동
  - [x] 멀티 모드일 때 leave 시 `lobbyController.leaveRoom()` 동시 송신
- [x] [lib/screens/result_screen.dart](../client_flutter/lib/screens/result_screen.dart) — 승/패 + Score / +Coin / +Point + 닉네임/Lv 표시 + 종료 사유 라벨 + 확인 버튼 → `/lobby`

### 라우팅 + 진입 흐름
- [x] [router.dart](../client_flutter/lib/router.dart) — `/game` + `/result` (extra=GameResult) 라우트 추가
- [x] **싱글플레이 진입** — LobbyScreen 의 "Play vs AI" 버튼 → `newImageList.json` 첫 row → `gameArgsProvider` 채우고 `/game`
- [x] **멀티플레이 진입** — BattleRoomScreen 에서 양측 ready 시 자동 — `newImageList.json` 첫 row 로 `GameStartArgs(opponentIsAi: false)` 후 `/game` (정식 mutual image id 라우팅은 W3 후속)

### 스킬 애니메이션
- [x] **placeholder** — 콤보 카운터 + 글로우 + 햅틱 으로 시각 피드백 대체
- [ ] **8 스킬 정식 에셋** — 디자이너 작업 + Lottie/Rive 통합 (§13 자산 입수 시)

### 테스트 — 14 추가 (총 88)
- [x] [test/game/image_set_test.dart](../client_flutter/test/game/image_set_test.dart) — JSON 파싱 4종 + `ImageCut.contains` 경계 + ImageSet.fromJson 1종 (총 9)
- [x] [test/util/constants_test.dart](../client_flutter/test/util/constants_test.dart) — comboScore/Time 경계 + calculateScore 승/패 + AI 시간 공식 (총 5)
- [x] [test/game/game_controller_test.dart](../client_flutter/test/game/game_controller_test.dart) — buildInitialGameState + 4 시나리오 (정답 콤보 + 시간보너스 / 오답 콤보리셋 + lastWrongTouch / 7개 클리어 → won / abort → opponentLeft) — Riverpod overrides + FakeWsClient 사용
- [x] **빌드 검증** — `flutter analyze` 0 issues, `flutter test` **88/88 통과** ✅, `flutter build apk --debug` 27.3s 통과 ✅

---

## 8. 인벤토리 / 상점 / 스킬트리 🟢 (2026-04-26 완료)

### 카탈로그
- [x] [lib/game/item_catalog.dart](../client_flutter/lib/game/item_catalog.dart) — `Items.java` 1:1 이식 (2013-12-20 기획)
  - `ItemType` enum (PEN/RING/BERRY/POTION/GOLD/ETC) + `fromTypeNo()` 범위 매핑 (안드 `classifyItemType` 동일)
  - `CatalogItem`(typeNo / type / power / price / money / maxNum) + `effectSeconds()`
  - 6 카테고리 const list — pen 3종 (50/100/200) / ring 2종 (80/150) / berry 6종 / potion 5종 (대용량 maxNum=1) / gold 4종 IAP (100/250/500/650 코인 × \$0.99/\$1.99/\$3.99/\$4.99) / etc 3종 (스킬덱 100/300/900)

### 상태
- [x] [lib/state/auth.dart](../client_flutter/lib/state/auth.dart) 확장 — `AuthState` 가 이제 user + items[] + skills{} + inventorySize + skillDeckCount 모두 보유
  - `_applyAuthBody` 가 login/guest/facebook 응답의 `item[]`/`skill[]` 자동 흡수
  - 액션 메서드: `applyWalletDelta` (coin/point/gem/hp/level), `applyInventoryDelta` (upsert/delete by quantity), `applySkillLearned`, `applySizeDelta`

### InventoryScreen
- [x] [lib/screens/inventory_screen.dart](../client_flutter/lib/screens/inventory_screen.dart)
  - [x] 6 카테고리 탭 (`ItemType.values`) + 카테고리별 그리드
  - [x] 인벤토리 칸 — 보유 아이템 + (inventorySize - 보유) 만큼 빈 슬롯 placeholder
  - [x] 셀 탭 → bottom sheet (사용 / 강화)
  - [x] 사용 → `spendMyItem.json` + `applyInventoryDelta`
  - [x] 강화 → `_FluxPickerDialog` (none / flux +5% / advanced_flux +10%) → `upgradeItem.json` + 결과 토스트
  - [x] 하단: 인벤토리 확장 (3..30) + 스킬덱 확장 (1..4) 버튼 — 한도 도달 시 disabled
  - [x] 상단: 코인/포인트/보석 잔액 칩 + Lv

### ShopScreen
- [x] [lib/screens/shop_screen.dart](../client_flutter/lib/screens/shop_screen.dart)
  - [x] 5 일반 카테고리 (PEN/RING/BERRY/POTION/ETC) + COIN(IAP) 별도 탭
  - [x] 잔액 표시 (코인/포인트/보석)
  - [x] 구매 확인 다이얼로그 (`buyitem` 문자열) → `buyItemAndSpendCoin.json` + 잔액·인벤토리 갱신
  - [x] 코인 부족 시 `lackcoin` 토스트
  - [x] 코인팩 (GOLD) 셀은 `\$0.99` 표시. 탭 시 §10 IAP 통합 위임 안내 (`msgIapIsNotInstalled`) — 실 결제는 §10 에서 활성화
  - [x] 셀: 효과 라벨 (+1.0s / -0.9s / +500 coin / Deck +1) + 가격 칩

### Upgrade dialog
- [x] InventoryScreen 의 `_FluxPickerDialog` — 3개 옵션 (none / flux / advanced_flux) → 서버 `upgradeItem.json fluxUsed` 파라미터로 전달 → `balance.js` 가 확률 보정 (성공률 0.5→0.1, +5/+10%) + `item_upgrade_log` 감사

### SkillTreeScreen
- [x] [lib/screens/skill_tree_screen.dart](../client_flutter/lib/screens/skill_tree_screen.dart)
  - [x] `skillCatalog.json` 응답 (44 스킬) → `SkillNode` 파싱
  - [x] 5계열 컬럼 (cat 🐱 / dog 🐶 / exp 💥 / flame 🔥 / snow ❄) × tier 9 행 grid
  - [x] 셀 상태: `learned` (체크 + primaryContainer) / `learnable` (level + point + 선행 모두 OK → 활성) / `locked` (level 미달 시 자물쇠)
  - [x] 학습 → 확인 다이얼로그 → `learnSkill.json` + `applySkillLearned` + 포인트 차감 표시
  - [x] 상단 포인트/Lv 칩

### 라우팅
- [x] [router.dart](../client_flutter/lib/router.dart) — `/inventory`, `/shop`, `/skills` 라우트 추가
- [x] LobbyScreen — 하단 IconButton 3개 (인벤/상점/스킬)

### 테스트 — 22 추가 (총 96)
- [x] [test/game/item_catalog_test.dart](../client_flutter/test/game/item_catalog_test.dart) — `fromTypeNo` 6 범위 + 2013 spec parity (price 배열 6개) + `effectSeconds` + `findByTypeNo` (총 16)
- [x] [test/state/auth_deltas_test.dart](../client_flutter/test/state/auth_deltas_test.dart) — applyWalletDelta 부분 갱신 / applyInventoryDelta upsert+delete+upgradeLevel / applySkillLearned + applySizeDelta (총 6)
- [x] **빌드 검증** — `flutter analyze` 0 issues, `flutter test` **96/96 통과** ✅, `flutter build apk --debug` 12.8s 통과 ✅

### 후속
- [ ] **§10 IAP 실 결제** — `in_app_purchase` 패키지 + StoreKit (Apple) + Google Play Billing 통합 + `verifyIap.json` 호출
- [ ] **인벤토리 칸 서버 일관성** — 현재 카테고리별 빈 슬롯이 `inventorySize` 만큼 펼쳐짐. 서버는 inventorySize = 전체 칸 수. UX 정책 결정 후 통합 그리드 또는 카테고리별 분배
- [ ] **스킬 학습 후 카탈로그 새로고침 흐름** — 현재는 단방향(local apply). 멀티 디바이스 사용 시 동기화 위해 주기적 `login.json` 재호출 또는 `/me.json` 추가

---

## 9. 랭킹 / 공지 / 캐릭터 화면 🟡

- [ ] `RankScreen` — 친구/전체 토글, 일/주 단위, 상승/하락 (`delta`)
- [ ] `NoticeScreen` — `flutter_html` 로 서버 응답 HTML 렌더
- [ ] `ProfileScreen` (구 캐릭터 화면) — 사진기 기능 (앨범/카메라 권한, `image_picker`), 캐릭터 변경

---

## 10. IAP — Apple StoreKit 추가 🔴

기존 (04-24.md §2): 서버 `iap.js` 가 Google + Samsung 검증 지원. **Apple 분기 추가 필요**.

### 클라 (Flutter)
- [ ] `in_app_purchase` 패키지 통합 — Google + Apple 동시 처리
- [ ] 4개 SKU 등록 (Google Play Console + App Store Connect):
  - `coin_100` / `coin_250` / `coin_500` / `coin_650`
- [ ] 결제 완료 → `purchaseToken` (Google) / `transactionReceipt` (Apple) 서버 전송
- [ ] 영수증 검증 응답에 따라 지갑 갱신 + Toast (`R.string.payment_complete` 대응)

### 서버
- [ ] `server/src/util/iap.js` — Apple 검증 추가 (`/verifyReceipt` 또는 App Store Server API v2)
- [ ] `iap_receipts.store` enum 에 `'apple'` 추가 (마이그레이션 필요)
- [ ] 환경변수: `APPLE_SHARED_SECRET` (또는 v2 의 issuer/keyId/privateKey)
- [ ] 상품 카탈로그 업데이트 — `productMap` 에 Apple SKU 매핑
- [ ] Sandbox vs Production endpoint 분기 (`is-sandbox` 플래그)

---

## 11. 푸시 알림 (FCM ↔ APNs) 🔴

기존 (04-24.md §2): 서버 측 FCM 송신 + `members.fcm_token` 저장 완료. **iOS 측 셋업 추가**.

### iOS 셋업
- [ ] Apple Developer Console — APNs Auth Key (`.p8`) 발급
- [ ] Firebase Console iOS 앱 등록 → APNs Auth Key 업로드
- [ ] `GoogleService-Info.plist` 다운로드 → `ios/Runner/` 배치
- [ ] `ios/Runner/Runner.entitlements` — `aps-environment` (development/production)
- [ ] `Info.plist` — `UIBackgroundModes` 에 `remote-notification` 추가

### 클라 (공통)
- [ ] `firebase_core` 초기화 (main.dart)
- [ ] `firebase_messaging` 토큰 획득 → `/registerFcmToken.json`
- [ ] iOS 권한 요청 (`requestPermission()` — alert/badge/sound)
- [ ] 포그라운드 / 백그라운드 / 종료 상태 메시지 처리
- [ ] 알림 클릭 → 대기실/배틀룸 deep link

---

## 12. WebSocket 마이그레이션 🟢 (2026-04-26 §6 와 함께 완료, 인증/heartbeat 후속)

iOS 백그라운드/앱전환 시 raw TCP 유지가 까다로워 WebSocket 으로 전환. 코드 100~107 메시지 의미는 보존.

### 서버
- [x] [server/src/socket/handlers.js](../server/src/socket/handlers.js) 신설 — TCP/WS 공유 핸들러 + 인메모리 상태
- [x] [server/src/socket/server.js](../server/src/socket/server.js) — TCP 어댑터로 슬림화 (디컴파일 APK 호환)
- [x] [server/src/socket/ws_server.js](../server/src/socket/ws_server.js) 신설 — `ws` 라이브러리 (`optionalDependency`), `attach(httpServer, {path:'/ws'})`
- [x] 메시지 프레임: 텍스트 `|code|arg1|arg2|...` (TCP 와 동일, 4자리 길이 prefix 만 제거 — 클라/서버 양쪽이 인식)
- [x] 같은 process 의 handlers 상태 공유 → TCP/WS 클라 동일 방에서 매칭 가능
- [x] WebSocket URL: 클라 `--dart-define=FINDIT_WS_URL=ws://10.0.2.2:22131/ws` (개발) / `wss://api.findit.example/ws` (운영, 별도 host:port 또는 reverse proxy)
- [ ] **인증** — 현재 protocol 은 첫 100 메시지의 userId 만 신뢰. session.js 와 결합해 `?token=<t>` 또는 첫 `|auth|<token>` 메시지 검증 (후속, 운영 전 필수)
- [ ] **heartbeat (ping/pong)** — `ws` 라이브러리의 자동 ping 활성 + 클라 측 idle keepalive (현재 OS 기본 + 재연결로 충분, 부하 테스트 후 결정)

### 클라 (Flutter)
- [x] [lib/api/ws_client.dart](../client_flutter/lib/api/ws_client.dart) — `WebSocketChannel.connect` 래퍼
- [x] reconnect — 지수 backoff (1→2→4→8→16→30s, 최대 5회 후 포기). `_wantConnected` 플래그로 사용자 의도 보존.
- [x] `onLifecycleChange(state)` — paused/detached/hidden 시 close (의도 유지), resumed 시 자동 reconnect
- [x] 메시지 라우팅 — `WsFrame` parse + broadcast Stream → `LobbyController` 가 listen (코드 7종 분기)

---

## 13. 자산 / 이미지 처리 🟡

- [ ] `pubspec.yaml` `assets:` 에 로컬 자산 등록 (스킬 아이콘·UI 이미지·임시 캐릭터)
- [ ] 디컴파일된 Android 자산 추출 — `android_project/app/src/main/res/drawable-*` PNG 임시 활용
- [ ] CDN 이미지 (스테이지/광고) — `cached_network_image` 로 다운로드 캐시
- [ ] 이미지 컨텐츠 manifest (04-24.md §5 / [CONTENT_PRODUCTION_GUIDE.md](CONTENT_PRODUCTION_GUIDE.md)) — 클라 측 hit test 용 dataclass `ImageCut(x, y, w, h)`
- [ ] 7개 imageCut 영역 hit test 로직 — `Rect.contains(Offset)` 단순 적용

---

## 14. iOS 특수 사항 🔴

- [ ] **권한 다이얼로그 문구** (`Info.plist`):
  - `NSCameraUsageDescription` (캐릭터 사진 촬영)
  - `NSPhotoLibraryUsageDescription` (앨범 선택)
  - `NSUserTrackingUsageDescription` (광고 ID — ATT 프롬프트)
  - `NSLocalNetworkUsageDescription` (개발 LAN 디버깅 — 운영 빌드는 제거)
- [ ] **App Tracking Transparency (ATT)** — iOS 14.5+ 광고 추적 허용 다이얼로그
- [ ] **Safe Area** — `MediaQuery.of(context).padding` 으로 노치/Dynamic Island 회피
- [ ] **Haptics** — `HapticFeedback.lightImpact()` (정답), `mediumImpact()` (오답)
- [ ] **다크모드** — 명시적 미지원 결정 (게임 화면은 자체 색상 — `MaterialApp.themeMode = ThemeMode.light`)
- [ ] **Orientation** — 가로 고정? (원본 디자인 확인 필요) — `SystemChrome.setPreferredOrientations`

---

## 15. 빌드 / CI / 출시 🔴

### 코드 서명
- [ ] iOS — Xcode 자동 서명 (개발) + `fastlane match` (CI) 또는 manual provisioning profile
- [ ] Android — 기존 키스토어 (또는 신규 발급) + `key.properties`

### CI/CD
- [ ] **Codemagic** (권장) 또는 GitHub Actions + Fastlane
- [ ] 워크플로:
  - `main` 푸시 → 두 플랫폼 빌드 → 테스트
  - 태그 `v*` → TestFlight + Play Internal Track 자동 업로드
- [ ] 환경변수 시크릿 관리 — Apple App-Specific Password, Google Play Service Account JSON

### 베타 / 출시
- [ ] TestFlight 내부 테스트 (100명까지 즉시)
- [ ] TestFlight 외부 테스트 (Apple 리뷰 1회 필요, 이후 10000명까지)
- [ ] Google Play Internal Track → Closed Track → Production
- [ ] 스크린샷 / 마케팅 자료 — 양 스토어 spec 다름 (iPad 도 필수)

---

## 16. 분석 / 광고 / 보조 🟡

- [ ] Analytics — `event.json` 호출 (`game_start`, `game_end`, `purchase`, `skill_learn` 등 04-24.md §2 와 동일)
- [ ] 광고 클릭 추적 — `adClick.json`
- [ ] 광고 보상 — `adReward.json` (24h 20회 상한)
- [ ] AdMob 통합 (선택) — `google_mobile_ads` 패키지, iOS 14.5+ ATT 필수
- [ ] Crashlytics — `firebase_crashlytics` 통합

---

## 17. 권장 작업 순서 (단계별)

| 단계 | 기간 | 산출물 |
|---|---|---|
| **W1**: §1 부트스트랩, §3 i18n, §4 REST 클라 | 1주 | 디바이스에 빈 앱 설치, 로그인 API 호출 성공 |
| **W2**: §5 로그인 화면, §6 대기실 골격, §11 푸시 (iOS 셋업) | 1주 | TestFlight 내부 빌드 1차, 한·영 토글 동작 |
| **W3**: §7 GameView 포팅, §12 WebSocket 마이그레이션 | 1.5주 | 2 디바이스 매칭 + 게임 1판 완주 |
| **W4**: §8 인벤토리/상점/스킬트리, §10 IAP | 1주 | Sandbox 결제 → 코인 적립 |
| **W5**: §9 랭킹/공지, §14 iOS 특수 사항, §16 분석 | 0.5주 | 기능 완성도 90% |
| **W6**: §15 베타 → 정식 출시 | 0.5주 | 양 스토어 출시 |

총 ~5–6주 (1인 풀타임 기준)

---

## 18. 빠른 레퍼런스

### 진실 자료 (Source of Truth)
- 게임 공식 / HP 테이블 / 점수 공식: [server/src/util/balance.js](../server/src/util/balance.js)
- 아이템 가격 / 효과: [Items.java](../android_project/app/src/main/java/com/findit/battle/Items.java) (2013 spec 적용본)
- 스킬 카탈로그: [server/sql/seed_skills.sql](../server/sql/seed_skills.sql)
- 이미지 컨텐츠 포맷: [CONTENT_PRODUCTION_GUIDE.md](CONTENT_PRODUCTION_GUIDE.md)
- i18n 키: [values/strings.xml](../android_project/app/src/main/res/values/strings.xml) + [values-ko/strings.xml](../android_project/app/src/main/res/values-ko/strings.xml) (양방향 145키)

### 핵심 상수 (포팅 시 그대로 옮길 값)
- `GAMETIME = 40` (초)
- `FINDRECT_TOTALNUM = 7`
- `SKILLWINDOW_TOTALNUM = 8` (덱 슬롯 풀)
- AI 발견 시간: `7 - level × 0.03` 초 ±15%
- 콤보 점수 보너스: 1/2/3/4+ → 100/200/400/700
- 콤보 시간 보너스: 1/2/3/4+ → 1/2/4/7 초
- 코인팩: 100 / 250 / 500 / 650 (\$0.99 / \$1.99 / \$3.99 / \$4.99)
- 인벤토리 확장: 3 → 30 칸 (단계당 +3, 9단계)
- 스킬덱 확장: 1 → 4 (3단계, 비용 100/300/900)
- 아이템 업그레이드: 0 → 9 단계 (확률 0.5 → 0.1)

### 서버 엔드포인트 베이스
- 운영 (TBD): `https://api.findit.example/`
- 개발 (에뮬): `http://10.0.2.2:8080/` (Android), `http://localhost:8080/` (iOS 시뮬)
- 어드민: `https://<server>/admin/ui` (X-Admin-Token 헤더)

### 디컴파일 reference 활용 가이드
> ⚠ `android_project/` 의 Java 소스는 jadx 디컴파일 결과로 변수명이 휴리스틱 치환되어 있음 (`r0`, `r5`, `mTimeCount` 등). **그대로 베끼지 말고**, 동작·룰·상수만 추출해서 Dart 로 깨끗하게 다시 씁니다. 게임 공식은 서버 `balance.js` 가 더 신뢰도 높음.

---

## 19. 결정 보류 / TBD

- [ ] 운영 도메인 / SSL 인증서 (`api.findit.example` 자리)
- [ ] CDN 선택 — GCS Public bucket vs CloudFront vs 자체
- [ ] Crash 리포팅 — Firebase Crashlytics vs Sentry
- [ ] 가로 vs 세로 — 원본 디자인 재확인 후 결정
- [ ] 다크모드 — 보류 결정 유지 vs 정식 지원
- [ ] 프로필 사진 (캐릭터 커스터마이징) — 1차 출시 포함 vs 후속
